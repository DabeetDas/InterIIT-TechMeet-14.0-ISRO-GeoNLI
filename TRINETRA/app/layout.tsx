import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "ISRO GeoNLI - Satellite Intelligence Platform",
  description: "Natural Language Interface for Geospatial Satellite Data Analysis by ISRO",
  keywords: ["ISRO", "satellite", "geospatial", "NLI", "earth observation", "remote sensing"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
